module.exports.loop = function() {
  console.log("Main functions");

  /*
    for each creep
      if (!creep)
        re-enqueue task
        delete creep
      else
        role_task(creep)
  */

  /*
  for each room
    if spawner.spawning == null
      queue.spawn()
  */
}
